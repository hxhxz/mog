-- Initial schema bootstrap for mog DB.
-- Real schema is managed by alembic (services/api/migrations); this file just ensures the DB exists.

-- Placeholder, alembic upgrade head will create all tables.
CREATE SCHEMA IF NOT EXISTS public;
